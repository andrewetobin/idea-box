<%= form_for(@idea) do |f| %>
  <ul>
    <% @idea.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>

 <%= options_from_collection_for_select(Category.all, :id, :name) %>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
